services:
  maturity:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - maturity-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/maturity.db

volumes:
  maturity-data:
